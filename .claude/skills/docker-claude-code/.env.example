# Claude Code CLI Configuration
# ====================
# 此文件为环境变量模板，由 init-docker-project.sh 自动生成 .env 时使用
# 复制此文件为 .env 并根据需要修改配置值，然后运行 init-docker-project.sh

# API Key 配置
# -------------------
# dummy 表示沿用宿主机的 API KEY(通过 --mount type=bind 挂载)
# 这是推荐的安全配置，避免在容器中存储真实的 API KEY
ANTHROPIC_API_KEY=dummy

# Claude Code Switch 本地代理端口
# -------------------
# 必须与 CC Switch 配置的代理端口一致
# Windows/macOS: 使用 host.docker.internal (Docker Desktop 自动支持)
# Linux: 需要在 docker-compose.yml 中配置 extra_hosts
ANTHROPIC_BASE_URL=http://host.docker.internal:15721

# 可选配置项
# ====================

# Claude Code CLI 版本(默认: latest)
# CLAUDE_CODE_VERSION=latest

# 工作目录挂载路径(默认: ./workspace)
# 这是项目代码的挂载点，容器内 /workspace 对应此目录
# WORKSPACE_PATH=./workspace

# Claude Code 配置持久化路径(默认: ./dev-home/config)
# 容器内 ~/.config/claude 对应此目录
# 包含 Claude Code 的配置文件(settings.json 等)
# CLAUDE_CONFIG_PATH=./dev-home/config

# Claude Code 用户目录持久化路径(默认: ./dev-home/claude)
# 容器内 ~/claude 对应此目录
# 包含 Claude Code 的用户数据和缓存
# CLAUDE_HOME_PATH=./dev-home/claude

# 平台特定说明
# ====================
# Linux 用户注意事项:
# - docker-compose.yml 中已配置 extra_hosts: "host.docker.internal:host-gateway"
# - 这确保容器能访问宿主机的 CC Switch 代理
# - 无需修改 ANTHROPIC_BASE_URL

# Windows/macOS 用户注意事项:
# - Docker Desktop 自动支持 host.docker.internal
# - 无需额外配置，使用默认值即可

# 安全提醒
# ====================
# - 切勿在 .env 文件中存储真实的 API KEY
# - .env 文件不应提交到版本控制系统
# - 确保 .env 文件权限设置为仅当前用户可读
